import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Стандартний output, який ідеально лягає на Vercel.
  // Тут можна додавати redirects, headers, images domains тощо.
};

export default nextConfig;
